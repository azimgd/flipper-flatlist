window.flatlistDebugger = {
  overscanBegin,
  overscanEnd,
  visibleBegin,
  visibleEnd,
  itemCount,
  scrollMetrics,
};

window.flatlistFlipper && window.flatlistFlipper.send('inspector', window.flatlistDebugger);