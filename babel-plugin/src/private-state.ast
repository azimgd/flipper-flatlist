window.flatlistDebugger = {
  overscanBegin,
  overscanEnd,
  visibleBegin,
  visibleEnd,
  itemCount,
  scrollMetrics,
};

window.flatlist && window.flatlist.send('inspector', window.flatlistDebugger);